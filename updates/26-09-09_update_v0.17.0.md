# v0.17.0 — responsive grid card editing

- Card elements use nine responsive grid cells again, with ordered drag-and-drop inside each cell.
- Manual mode has a fullscreen workspace that uniformly enlarges the active card.
- The editor preview uses the current card width and height, including live toolbar resizing changes.

Validation: `npm test` in `test/` — 738 checks passed. Offscreen Chromium layout and effects checks passed.
